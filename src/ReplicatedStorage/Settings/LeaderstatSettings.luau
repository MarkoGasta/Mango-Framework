return {
    LOAD_WAIT_TIME = 5,

    --[=[
        Contains data about leaderstats that can be utilized in the game.
        Example for leaderstat data.

        ```lua
            LEADERSTAT_DATA = {
                KILLS = {
                    RICH_NAME = "Kills",
                },
            }
        ```
    ]=]
    LEADERSTAT_DATA = {}
}